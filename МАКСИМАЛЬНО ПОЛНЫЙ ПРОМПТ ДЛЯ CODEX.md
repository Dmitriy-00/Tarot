
# ⭐ **МАКСИМАЛЬНО ПОЛНЫЙ ПРОМПТ ДЛЯ CODEX: САЙТ ТАРО (ЛИМИНАЛЬНАЯ КОЛОДА)**

**Создай полноценный веб-сайт раскладов для “Лиминальной Колоды Метасистемы”.
Это НЕ классическое таро — это система из 4 типов карт: Узлы (Nodes), Сцены (Spaces), Фигуры (Figures), Операции (Operations). Сайт должен визуализировать расклад как маршрут по лиминальному космосу психики.**

---

# 🎯 **ЦЕЛИ ПРОЕКТА**

1. Реализовать сайт-движок раскладов Лиминальной Колоды Метасистемы.
2. Использовать Next.js + React + TypeScript.
3. Применить TailwindCSS или Styled-Components.
4. Поддержать анимации (Framer Motion или CSS keyframes).
5. Реализовать JSON-модель карт и раскладов.
6. Создать визуальный интерфейс в стиле тёмного техно-лиминального портала.
7. Сделать UI расклада: карты = порталы, маршрут = граф.
8. Страницы: Главная, Выбор расклада, Экран расклада, Просмотр карты.

---

# 📁 **СТРУКТУРА ПРОЕКТА**

Создай проект со структурой:

```
/src
  /app
    page.tsx               // главная
    /spread
      page.tsx             // выбор расклада
    /reading
      /[id]
        page.tsx           // конкретный расклад
    /card
      /[id]
        page.tsx           // подробный просмотр карты
  /components
    Header.tsx
    Footer.tsx
    PortalSphere.tsx
    LiminalBackground.tsx
    SpreadSelector.tsx
    CardPortal.tsx
    CardViewer.tsx
    SpreadGraph.tsx
    SpreadActions.tsx
  /lib
    deck.ts                // JSON колоды
    spreads.ts             // пресеты раскладов
    generator.ts           // логика генерации раскладов
  /styles
    globals.css
    animations.css
```

---

# 🎴 **1. МОДЕЛЬ КОЛОДЫ**

Создай JSON-описание карт:

```
type Card = {
  id: string;
  type: "node" | "figure" | "operation" | "scene";
  name: string;
  subtitle?: string;
  meaning: {
    esoteric: string;
    psychological: string;
    action: string;
  };
  topology: string[];
  energy: {
    D: number;
    C: number;
    I: number;
  };
  image?: string;
};
```

Загрузи карты из отдельного файла `deck.ts`.

---

# 🔮 **2. МОДЕЛЬ РАСКЛАДОВ**

Создай пресеты раскладов:

```
type SpreadPattern = {
  id: string;
  name: string;
  positions: {
    id: string;
    label: string;
    type?: "node" | "figure" | "operation" | "scene";
  }[];
};
```

Примеры раскладов:

### 1) "Тропа Ходока"

* Node
* Operation
* Figure

### 2) "Пороговая Комната"

* Node
* Scene
* Figure
* Operation

### 3) "Лиминальный Маршрут"

5–7 карт по дуге.

---

# 💫 **3. ГЕНЕРАТОР РАСКЛАДОВ**

В файле `generator.ts` создай функцию:

```
export function generateSpread(pattern: SpreadPattern, deck: Card[]): Reading {
   // берёт карты подходящих типов
   // случайным образом или по заданным правилам
}
```

---

# 🌑 **4. ГЛАВНАЯ СТРАНИЦА**

Компоненты:

* `LiminalBackground`: тёмный фон с туманом и топологическими линиями
* `PortalSphere`: большая анимационная сфера-портал
* Текст: «Войти в Лиминальное Пространство»
* Кнопки: «Сделать расклад», «Исследовать карты»

Эстетика: тёмная, космо-минимальная, лёгкие свечения.

---

# 🌀 **5. ВЫБОР РАСКЛАДА**

`SpreadSelector.tsx`:

* три крупных портала-кнопки
* анимированные световые разрывы
* подписи:

  * Тропа Ходока
  * Пороговая Комната
  * Лиминальный Маршрут

---

# 🜄 **6. ЭКРАН РАСКЛАДА (reading/[id])**

Главная часть сайта.

Состоит из:

### 1) Линии-связки (граф расклада)

`SpreadGraph.tsx`

* кривые линии между картами
* движение свечения по линиям

### 2) Карты как порталы

`CardPortal.tsx`

Каждая карта — это:

* полупрозрачная пластина
* фон-сцена (png/jpg)
* шейп типа карты (svg)
* неоновая окантовка
* анимация «дрейфа» или «дыхания»

### 3) Детальная панель справа

`SpreadActions.tsx`

* значение карты
* психологическая интерпретация
* практическое действие
* связи с другими картами

---

# 🜁 **7. СТРАНИЦА КАРТЫ (card/[id])**

`CardViewer.tsx`:

### Включает:

* Большая версия карты
* Анимация появления (glow → reveal)
* Подсветка топологии (D/C/I)
* Смысловой блок
* Кнопка «Назад к раскладу»

---

# 🎨 **8. UI-KIT (ВИЗУАЛЬНЫЕ КОМПОНЕНТЫ)**

Используй Tailwind или Styled Components.

### Цвета:

* `#0A0B11` — void
* `#131728` — deep indigo
* `#6EF6FF` — cyan glow
* `#BC7CFF` — violet glow
* `#FF7C6B` — conflict red
* `#EAEFF8` — soft white

### Эффекты:

* backdrop-blur
* прозрачные стеклянные панели
* туман (CSS + png overlay)
* топологические линии (svg)

### Кнопки:

* тонкие неоновые линии
* состояния hover: glow, ripple

### Карточки:

* тёмные
* текстуры тумана
* svg-шейпы
* лёгкое дыхание (animation: pulse 12s infinite)

---

# 🔥 **9. АНИМАЦИИ (Framer Motion)**

### 1. `breathingVoid`

фон увеличивается на 1–2%

### 2. `mistDrift`

туман смещается по синусоиде

### 3. `glyphReveal`

svg рисуется по stroke-dashoffset

### 4. `portalPulse`

карта слегка увеличивается и светится

### 5. `graphTrace`

линия расклада рисуется от карты к карте

---

# 🧱 **10. СГЕНЕРИРУЙ КОД**

Codex должен:

* сгенерировать весь проект
* создать компоненты
* создать JSON-модели
* прописать логику расклада
* подключить анимации
* построить UI
* сделать навигацию
* собрать темы и стили
* использовать mock-изображения карт

---

# ✔️ **ИТОГОВОЕ ЗАДАНИЕ ДЛЯ CODEX**

**Сгенерируй полностью рабочий сайт Next.js + React для Лиминальной Колоды Метасистемы, строго следуя специфике выше: UI, модели данных, расклады, анимации, компоненты, структуры файлов, визуальный стиль, логика переходов и просмотр карт. Создай максимальное количество кода, полностью готового к запуску.**
